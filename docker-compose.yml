services:
  langflow:
    image: langflowai/langflow:latest
    container_name: langflow
    ports:
      - "7860:7860"
    environment:
      - LANGFLOW_ENV=prod
    volumes:
      - langflow_data:/app/langflow

volumes:
  langflow_data:
